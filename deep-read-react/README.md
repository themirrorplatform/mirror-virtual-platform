# The Deep Read — React immersive site

A Vite + React landing page with immersive feed-style scrolling, motion, magnetic buttons, and reusable sections.

## Run locally
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```

Deploy the project folder to Vercel or Netlify. The static support pages live in `/public`.
